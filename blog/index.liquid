---
title: My Blog
layout: main
---

{% for post in collections.post reversed %}
  <div class="excerpt">
    <a href="{{post.url}}">{{ post.data.title }}</a>
    - {{ post.date | nicedate }}<br/>
    {% excerpt post %}
  </div>
{% endfor %}