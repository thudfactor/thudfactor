---
import Page from "@components/PageItems/Page.astro";
import Content from "@components/PageItems/Content.astro";
import EndMark from "@components/PageItems/EndMark.astro";

interface Props {
	title: string;
	description: string;
	date: Date;
	updatedDate?: Date;
	cover?: any;
}

const { title, description, cover } = Astro.props;
---

<Page title={title} description={description} cover={cover}>
	<Content {...Astro.props}>
		<slot />
		<EndMark />
	</Content>
</Page>
